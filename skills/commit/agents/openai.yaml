interface:
  display_name: "Commit"
  short_description: "Create atomic commits using repository conventions"
  default_prompt: "Use $commit to create atomic commits that follow this repository's conventions."
