interface:
  display_name: "Quality Gate"
  short_description: "Run project checks and report evidence"
  default_prompt: "Use $quality-gate to run the repository checks and report PASS, FAIL, or INCONCLUSIVE with evidence."
